# Services definition for building & deploying

x-common:

  docs-build-context: &docs-build-context
    # Build context
    context: .
    dockerfile: ./Dockerfile

  docs-environment: &docs-environment
    # Deployment
    APP_ENV: ${APP_ENV:-prod}
    GITHUB_REPOSITORY_URL: ${GITHUB_REPOSITORY_URL:-}
    GITHUB_SHA: ${GITHUB_SHA:-}
    VERSION_TAG: ${VERSION_TAG:-}
    # Astro
    ASTRO_SITE_URL: ${ASTRO_SITE_URL:-}
    ASTRO_BASE_PATH: ${ASTRO_BASE_PATH:-}
    ASTRO_ASSETS_PREFIX: ${ASTRO_ASSETS_PREFIX:-}
    # Application
    # (Add env vars for your application here.)

services:

  docs:
    # Build
    image: ${IMAGES_PREFIX:-app}:${IMAGES_TAG:-latest}
    build:
      <<: [ *docs-build-context ]
      target: app_prod
      args:
        PORT: ${APP_DOCKER_PORT:-8080}
        BUILD_CHECK: ${BUILD_CHECK:-true}
        NGINX_HOST: ${NGINX_HOST:-localhost}
        <<: [ *docs-environment ]
    # Deploy
    environment:
      <<: [ *docs-environment ]
    ports:
      - ${DOCS_PORT:-8080}:${DOCS_DOCKER_PORT:-8080} # HTTP
