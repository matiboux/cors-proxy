# Services definition override for development

services:

  docs:
    # Build override
    image: ${IMAGES_PREFIX:-app}-docs-dev:${IMAGES_TAG:-latest}
    build:
      target: app_dev
      args:
        UID: ${DOCKER_UID:-${UID:-1000}}
    # Deploy override
    user: ${DOCKER_UID:-${UID:-1000}}
    develop:
      watch:
        # Docker image
        - action: rebuild
          path: ./docs/Dockerfile
        # Application build files
        - action: rebuild
          path: ./docs/app/pnpm-lock.yaml
        # Application source code
        - action: sync
          path: ./docs/app
          target: /app
