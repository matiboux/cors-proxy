# Services definition override for development

services:

  app:
    # Build override
    image: ${IMAGES_PREFIX:-app}-dev:${DOCKER_TAG:-latest}
    build:
      target: app_dev
    # Deploy override
    develop:
      watch:
        # Docker image
        - action: rebuild
          path: ./app/Dockerfile
        # App source files
        - action: sync+restart
          path: ./app/app
          target: /app
          ignore:
            - node_modules/
